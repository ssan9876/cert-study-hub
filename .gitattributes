# Normalize line endings to LF in the repository for all text files so the
# project is consistent across Windows/macOS/Linux checkouts.
* text=auto eol=lf

# Treat common binary assets as binary (no EOL conversion).
*.png binary
*.jpg binary
*.svg text eol=lf
